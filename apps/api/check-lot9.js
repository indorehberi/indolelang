const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const lot = await prisma.lots.findFirst({ where: { lot_number: 9 }, include: { bids: { orderBy: { amount: 'desc' } } } }); console.log('LOT:', JSON.stringify(lot, null, 2)); if (lot) { const logs = await prisma.audit_logs.findMany({ where: { resource_type: 'lots', resource_id: lot.id }, orderBy: { created_at: 'asc' } }); console.log('LOGS:', JSON.stringify(logs, null, 2)); const invoices = await prisma.invoices.findMany({ where: { lot_id: lot.id } }); console.log('INVOICES:', JSON.stringify(invoices, null, 2)); } } main().catch(console.error).finally(() => prisma.$disconnect());
