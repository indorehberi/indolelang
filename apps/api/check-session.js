const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const lots = await prisma.lots.findMany({ where: { session_id: '4157e33e-3dff-4d4d-8508-a0e90844f3db' }, orderBy: { lot_number: 'asc' } }); console.log('LOTS:', JSON.stringify(lots.map(l => ({ num: l.lot_number, status: l.status, updated: l.updated_at, created: l.created_at })), null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
